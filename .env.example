# Optional frontend build configuration.
# The Google OAuth Web Client ID is intentionally a public client identifier.
# If omitted, the application falls back to the configured project client ID.
VITE_GOOGLE_CLIENT_ID=
