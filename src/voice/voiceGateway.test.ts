import {describe,it,expect,vi} from 'vitest'; import {createVoiceGateway,VoiceProvider} from './voiceGateway';
describe('Stage 5 voice gateway',()=>{it('requests microphone permission safely',async()=>{const old=navigator.mediaDevices;Object.defineProperty(navigator,'mediaDevices',{value:{getUserMedia:vi.fn(async()=>({getTracks:()=>[]}))},configurable:true});const p={} as VoiceProvider;expect(await createVoiceGateway(p).requestMicrophone()).toBe(true);Object.defineProperty(navigator,'mediaDevices',{value:old,configurable:true});});it('routes trimmed speech text',()=>{const g=createVoiceGateway({} as VoiceProvider);expect(g.routeRequest({text:'  hello  '} ).text).toBe('hello');});});
