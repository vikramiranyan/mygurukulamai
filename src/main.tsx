import React,{useEffect,useMemo,useState}from'react';import{createRoot}from'react-dom/client';import'./styles.css';import{credentialToSession,isSessionValid,type AuthSession}from'./auth/googleAuth';import{BrowserVoice}from'./voice/browserVoice';import{createTeacherTurn}from'./teachers/teacherCurriculumVoice';type Subject={name:string;teacher:'Vikram'|'Raji';icon:string;chapters:string[]};const subjects:Subject[]=[];function App(){const[session,setSession]=useState<AuthSession|null>(null);if(!session)return <div className="login-screen" role="main"><img className="login-master-art" src="/update-001-login.svg" alt="Gurukulam AI login screen with two children learning together"/><button className="login-google-hitarea" aria-label="Continue with Google"/></div>;return <div/>}createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);