import{describe,expect,it}from'vitest';import{LessonConversation,normalizeChildResponse}from'./lessonLoop';
describe('Stage 8 lesson conversation',()=>{it('preserves teacher-child turn continuity',()=>{const l=new LessonConversation();l.start('Hello child! Let’s learn Addition.');l.teacher('ask','What is 1 + 1?');const s=l.child('  Two  ');expect(s.lastChildText).toBe('Two');expect(s.phase).toBe('check');expect(s.turn).toBe(3);expect(s.history).toHaveLength(3)});it('moves a failed turn into reteach recovery',()=>{const l=new LessonConversation();l.start('Let’s learn Addition.');l.teacher('ask','What is 1 + 1?');l.child('I do not know');expect(l.recover().phase).toBe('reteach');});it('normalizes child speech text',()=>expect(normalizeChildResponse('  two   apples  ')).toBe('two apples'));});
